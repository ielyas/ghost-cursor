# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
*.xcworkspace/xcuserdata/
GhostCursor.xcodeproj/xcuserdata/
GhostCursor.xcodeproj/project.xcworkspace/xcuserdata/

# Swift Package Manager
Core/.build/
Core/.swiftpm/
.swiftpm/

# Internal planning (keep local; not for the public repo)
PROJECT_SPEC.md
plans/

# Release build and publish artifacts
dist/

# Wrangler local state (R2 uploads authenticate through the CLI's own session)
.wrangler/

# Cursor hook runtime state (per-machine; rewritten every turn)
.cursor/hooks/state/
